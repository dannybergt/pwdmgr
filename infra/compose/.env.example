# Copy to infra/compose/.env (git-ignored). These values only ever configure the local
# development/evaluation stack; a real deployment sets them through its secret store.
POSTGRES_PASSWORD=changeme-dev-only
SEED_ADMIN_PASSWORD=changeme-dev-only
# Optional overrides
#ASPNETCORE_ENVIRONMENT=Development
#SEED_ENABLED=true
#PWDMGR_PROXY_NETWORK=172.16.0.0/12
#GIT_SHA=local
